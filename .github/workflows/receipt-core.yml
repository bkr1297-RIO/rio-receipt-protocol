name: Receipt core

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install Vesper fixture dependency
        run: python -m pip install cryptography==46.0.0
      - name: Initialize isolated local unit
        run: npm run init -- --owner "human:ci"
      - name: Run declared full suite
        run: npm run test:full
      - name: Generate and verify a receipt
        run: |
          node generate_receipt.js
          node verify_receipt.js examples/valid_receipt.json
